// BYS Collections - Masthead Button Component (DEPRECATED)
// This module has been removed. The sidebar (Guide) button is now the only entry point.
// File kept as empty stub to prevent any residual import errors.

window.BYS = window.BYS || {};
window.BYS.Collections = window.BYS.Collections || {};
window.BYS.Collections.MastheadButton = {
  inject: () => false,
  remove: () => {},
  exists: () => false
};
